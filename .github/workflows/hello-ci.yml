name: Hello GitHub Actions
on:
  push:
    branches:
      - main
      - feature/*-
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: echo "Hello from GitHub Actions!"
      - name: Print greeting
        run: echo "Hello from Vedanshu"
      - name: Show date
        run: date
      - name: Show operating system
        run: uname -a
